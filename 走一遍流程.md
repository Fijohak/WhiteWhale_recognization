# WhiteWhale 当前流程实跑手册

所有命令都在仓库根目录执行。正式配置统一读取 `configs/pipeline.yaml`；CLI 参数只作
本次覆盖。原图只读，所有生成物写入 `outputs/`。

## 一、运行前检查

```powershell
python --version
python -m pip install -r requirements.txt
python -m pytest tests -q

Test-Path "src_dataset"
Test-Path "models/detectors/yolov8n_dorsalfin.pt"
Test-Path "outputs/metric_learning/r4/best.pt"
```

三项 `Test-Path` 必须为 `True`。本项目的 `src_dataset` 可能是指向数据盘的目录联接；
链接本身存在但目标盘未挂载时，读取原图仍会失败，应先恢复数据盘，不能改代码绕过。

设备默认使用 `auto`：有可用 CUDA 时走 GPU，否则回退 CPU。无需修改源码中的
`det_device`；需要强制设备时使用相应入口的 CLI 参数或配置。

## 二、数据索引与批次内确认个体库

```powershell
python scripts/prepare_data.py scan
python scripts/prepare_data.py build-pilot
```

主要产物：

- `outputs/index/dataset_manifest.csv`
- `outputs/index/dataset_stats.json`
- `outputs/pilot/pilot_set.csv`
- `outputs/pilot/pilot_set_stats.json`

高分目录数字子文件夹中的 `individual_id` 是批次内已确认个体，可作批内监督标签；
ID 含 session 命名空间，不表示跨批次同名编号已经对齐。多图通常来自短时连拍，
训练和评估必须整串隔离。完整同串键是
`session_id + 文件名序列键 + 连续帧段`，连续帧段按相邻帧号差 `<=2` 的传递闭包生成；
不能只比较末尾帧号，也不能把跨批次样本默认当异体。

当前 ID schema 为 `source_label_preserving_v2`：来源目录 `05` 保持为 `_05`，不会被
数值解析成 `_5.0`。旧 ID 到当前 ID 的一次性映射在
`outputs/pilot/individual_id_migration_v1_to_v2.csv`，旧 pilot 备份在
`outputs/pilot/pilot_set_legacy_ids_20260829.csv`。

## 三、校验或版本化重建历史库 r4 特征

```powershell
python scripts/run_cross_time_batch.py --only-gallery
```

该命令只读校验配置指向的活动 gallery，不会原地重建。当前活动严格产物为：

- `outputs/artifacts/r4_yolocrop_v3/gallery/embeddings.npy`
- `outputs/artifacts/r4_yolocrop_v3/gallery/embeddings_meta.csv`
- `outputs/artifacts/r4_yolocrop_v3/gallery/embeddings_config.json`
- `outputs/artifacts/r4_yolocrop_v3/bundle.json`

确需从 20140806 历史库原图重新执行 YOLO 裁剪和 r4 特征提取时，发布到新的不可覆盖
目录，例如：

```powershell
python scripts/rebuild_r4_artifacts.py --out outputs/artifacts/r4_yolocrop_v4
```

重建完成并验收后，再显式修改 `configs/pipeline.yaml` 切换活动目录。不要覆盖 v3，
也不要把尚未通过全量评估的候选模型直接接入生产。

新提取的 config 会记录 embedding/meta/权重 SHA-256、模型、裁剪/预处理、源码提交和
运行环境。`embeddings.npy`、`embeddings_meta.csv` 与 `embeddings_config.json` 必须作为
一组使用，不能只复制其中一个文件；缺少 provenance、哈希不符或 checkpoint 不同均应
停止运行，不能手工绕过。

## 四、运行新批次归档

先跑一个批次：

```powershell
python scripts/run_cross_time_batch.py --sessions "20140419 02" --skip-gallery
```

去掉 `--sessions` 可处理配置中的全部新批次。`--skip-gallery` 表示复用上一步历史库；
若特征或 meta 缺失/不配套，程序会明确报错，不应手工拼接不同版本产物。

每批输出位于 `outputs/cluster_archival/cross_time/<批次>/`，包括逐图聚类、簇级
候选匹配、代表图、审核拼图和摘要。聚类与跨时间匹配均是 Candidate，不自动写成
确认身份。

也可直接跑单批正式入口：

```powershell
python scripts/run_pipeline.py --input-manifest outputs/index/dataset_manifest.csv `
  --session "20140419 02" --batch-name "20140419 02" --sheets
```

## 五、多人独立审核与裁决

每位审核人使用唯一名称和不同端口，审核前互不查看他人票据。例如：

```powershell
python scripts/launch_review.py `
  --clusters "outputs/cluster_archival/cross_time/20140419 02/clusters.csv" `
  --batch-embeddings "outputs/cluster_archival/cross_time/20140419 02/embeddings.npy" `
  --reviewer "reviewer_a" --port 8001
```

其余审核人改为 `reviewer_b` / `reviewer_c` 和其他端口。所有人的原始票按
`image_id + reviewer` 并存写入 annotations，不互相覆盖；网页只加载当前审核人的票，
保持盲审。

至少三人完成后，由未指定个人审核人的导出命令生成保守裁决：

```powershell
python scripts/launch_review.py --export `
  --clusters "outputs/cluster_archival/cross_time/20140419 02/clusters.csv" `
  --min-reviewers 3
```

只有人数达标且结论完全一致的图片进入 `confirmed_individuals.csv`；人数不足或存在
任意分歧均保留为 pending/conflict，不强行归档。原始票和裁决摘要分别保留，便于追溯。

## 六、单图查询

```powershell
python scripts/launch_query.py
```

打开 `http://127.0.0.1:8000`，上传照片后得到 known / unknown 二态候选提示与
Top-K 候选。系统会严格检查查询模型与 gallery 的模型、裁剪/预处理、checkpoint 及
embedding/meta 哈希。当前阈值状态是 `provisional_unvalidated`，旧跨批次实验不能提供
正式 FA：必须在跨批次人工对齐后的独立集上重新标定。unknown 只表示“疑似未知”，
不是新个体的自动确认。

## 七、r5 候选的当前状态

r5 已完成真实训练与严格评估。训练内小验证集从预训练基线 R@1 0.682 升到最佳
0.955，但只有 22 张可评 query，因此只用于选择 checkpoint。75 个批次内身份的同协议
全量评估（97 query / 255 gallery）中，r5 的 R@1/mAP 为 0.412/0.580，低于 r4 的
0.567/0.707；E5 保守口径为 0.372/0.458（r4 0.465/0.564），串抽样口径为
0.279/0.402（r4 0.419/0.534）。所以当前生产仍使用 r4，不能切换到 r5。

主要审计产物：

- `outputs/metric_learning/r5_candidate/metrics.json`
- `outputs/embeddings/embeddings_eval51_all_r5_candidate_v1.npy` 及相邻 meta/config
- `outputs/verification/r4_full_manifest_v2/`
- `outputs/verification/r5_candidate_v1/`
- `outputs/reports/cluster_retrieval_r5_candidate_v1_full_manifest/`

这些都是批次内、短时间间隔、完整同串约束下的评估，不回答跨月或跨年识别能力。

## 八、当前不能自动完成的事项

- 当前项目数据按负责人说明默认已授权；外部补充数据必须另行核对授权、许可和用途，
  可接受错误合并率仍属于科研决策；
- A9 序列抽样、历史库二次一致性核验和争议照片裁决需要人工看图；
- 当前标签虽为批次内已确认个体，但时间间隔短。没有独立跨日期真值前，任何短间隔
  指标都不能外推为跨月或跨年能力；
- 跨批次 individual_id 尚未对齐，跨批次相似度只能生成 `possibly_same` 候选，不能
  充当 confirmed-different/unknown 负例或用于报告正式 FA；
- 原图盘当前已接入，是检测、特征重提取和逐图漏检诊断的运行前提；当前漏检诊断
  证据已于 2026-08-29 重建，若之后盘符不可用则不能重跑。
