# 实验日志（EXPERIMENT_LOG）

> 每次实验记录配置、结果、结论；所有指标均为弱标签一致性（Source Group ≠ Confirmed Individual），不称识别率。
>
> **2026-08-24 重构说明（路径迁移，不影响历史结论）**：正式管线逻辑已收敛到 `src/whitewhale/`，
> 本日志中提到的旧脚本路径（`scripts/scan_dataset.py`、`scripts/extract_embeddings.py`、
> `scripts/train_metric_learning*.py`、`scripts/extract_r3_yolocrop.py`、`scripts/pipeline_archival.py`、
> `scripts/detect_and_crop.py`、`scripts/fiftyone_review.py`、`scripts/review_app.py` 等）已迁移或删除，
> 对应功能现由 `scripts/prepare_data.py` / `scripts/run_pipeline.py` / `scripts/train_reid.py` /
> `scripts/launch_review.py` / `scripts/launch_query.py` / `scripts/evaluate.py` 提供
> （详见 README）。一次性实验脚本在 `experiments/`。实验配置、结果与结论保持原样。

## 2026-08-17 实验 E1：YOLOv8 背鳍检测器（导师推荐方案）

**背景**：导师建议引入 YOLOv8。项目定位更新为"加速数据处理"（批内自动归档 + 代表图选择），YOLOv8 作为上游背鳍检测器替换中心裁剪假设（crop_center.py 的 0.45 中心窗）。

**配置**：
- 数据：pilot 199 张，SAM vit_b 自动掩码辅助预标注 → 人工抽查剔除 30 张（exclude.txt）→ **167 张**训练（134 train / 33 val，按 Sequence 划分，全局约束 3）
- 模型：YOLOv8n（yolov8n.pt 预训练初始化），imgsz=1024，epochs=100（早停于 62），fliplr=0（约束 4）
- 环境：RTX 4060 Laptop 8GB，ultralytics 8.4.120
- 标注语义：框 = 背鳍及周围身体区域；用户抽查确认 16/20 可用（框歪/未框完可接受）

**结果 1：检测性能（val 33 张）**
- mAP50 = 0.635，mAP50-95 = 0.359，P = 0.733，R = 0.581
- 权重：`models/detectors/yolov8n_dorsalfin.pt`

**结果 2：检索对比（B 口径 leave-one-out，弱标签一致性）**

| 特征来源 | R@1 | R@5 | R@10 | mAP |
|---|---|---|---|---|
| 预训练 · 整图 | 0.304 | 0.592 | 0.696 | 0.372 |
| 预训练 · 中心裁剪 | 0.304 | 0.592 | 0.696 | 0.372 |
| 预训练 · YOLO 检测裁剪 | 0.310 | 0.549 | 0.614 | 0.364 |
| 微调 · 整图 | 0.473 | 0.728 | 0.793 | 0.541 |
| 微调 · 中心裁剪 | 0.440 | 0.728 | 0.777 | 0.539 |

**结论**：特写图上检测裁剪 ≈ 中心裁剪 ≈ 整图（打平）。80 分以上照片背鳍本已居中，检测无增量；检索瓶颈在特征判别性而非定位。

**结果 3：散图泛化（202 张 70-79 散图，真实目录实扫）**
- 检出 183 / 202（90.6%），19 张回退中心裁剪；检出置信度中位 0.556，>0.5 占 63.9%
- 结论：检测器在非特写图上不依赖居中假设，泛化可用 → 支撑"散图自动归档"工作流

**过程中修复的环境/脚本问题（记录备查）**：
1. pip 代理失败 → 清华镜像；ultralytics 启动在线同步卡死 → settings sync=False
2. data.yaml 相对 path 基于 cwd 解析（中文路径不稳定）→ 运行时生成绝对路径
3. train.txt 缺扩展名（img2label_paths 匹配 `\images\` 子串需要绝对反斜杠路径）→ 清单写绝对路径
4. SAM 8GB 显存 OOM → 输入缩放 1024 + bbox 映射回原图
5. 散图 manifest relative_path 缺 session 前缀（5 张路径在子文件夹）→ 目录实扫重建清单

**待办**：散图场景检索对比（中心裁剪 vs YOLO 裁剪的归档命中率）；EXPERIMENT_LOG 后续按实验续写。

## 2026-08-17 实验 E2：散图归档场景检索对比（TASKS 6.1c）

**背景**：验证"加速归档"主路径——输入散图池，把同体照片聚到一起、每簇取代表图归档。散图无个体归属标签（批内有主但具体归属未确认），采用两个可验证口径；特征统一为 MegaDescriptor-T-224 预训练（与 assign_pool 同模型，无泄漏），YOLO 分支的 19 张 fallback 图特征替换为中心分支（变量纯净）。

**配置**：`scripts/eval_pool_archival.py`，202 张散图（01:54 / 03:148），中心裁剪 0.55 vs YOLO 检测裁剪（非均匀扩展）。

**口径 A：相邻帧互检（弱正样本 81 对 = 同批连拍帧号差 ≤2，视为同一次目击）**

| 指标 | 中心裁剪 | YOLO 裁剪 |
|---|---|---|
| pair_hit@1 | 0.494 | 0.432 |
| pair_hit@5 | 0.630 | 0.580 |
| pair_hit@10 | 0.691 | 0.716 |

McNemar 精确二项 p=0.359（@1）→ **无显著差异**。注：弱标签有噪声（相邻帧可能拍到群内不同个体），命中率是下限估计；YOLO @1 略低可能源于检测框帧间抖动。

**口径 B：归档把握度（散图 → 同批已确认个体 Top-1 相似度，n=202）**

| 指标 | 中心裁剪 | YOLO 裁剪 |
|---|---|---|
| Top1 中位 | 0.812 | **0.866** |
| Top1 ≥0.6 | 96.0% | 98.5% |
| Top1 <0.55（低置信） | 3.5% | 1.0% |

Wilcoxon 配对检验 p=3.4e-16（差值中位 +0.045）→ **极显著**：YOLO 检测裁剪让散图检索已确认个体的分数系统更高，归档判断更有把握。

**结论**：
- 特写图（E1）：检测裁剪无增量；散图（E2）：检测裁剪显著提升归档把握度（Top1 分数），低置信比例降到 1/3——检测器的价值在"背鳍不居中的非特写图"上体现；
- 相邻帧互检两法相当 → 检测裁剪不影响"同体聚簇"能力，且分数更高，归档链路推荐检测裁剪；
- 产物：`outputs/reports/pool_archival/`（metrics.json + A/B 原始配对数据，检验可复现）；特征 `outputs/embeddings_pool_archival/`。

## 2026-08-17 实验 E3：跨群未知个体预演（TASKS 6.7 第一步）

**背景**：预演"疑似新个体"判定流程——新批次到达时，库外个体应被拒识为"疑似新个体"而非误配到已知个体。设计：历史库=01 群（14 个体 33 张），"新批次"=03 群（29 个体 166 张，对 01 库全部为真库外），反向对称再测。这是**跨群未知个体**预演（新群到达场景），不是跨时间新个体出现（需多天数据）。脚本 `scripts/eval_openset_preview.py`。

**三分布口径**：known+ = 库内同体照片间最大相似度（多图个体）；known- = 库内异体间最大相似度；unknown = 库外 query 对库内 gallery 最大相似度。防错并优先：阈值选择压 open-set FA，宁可多标候选。

**结果 1：预训练特征（MegaDescriptor）**

| 方向 | known+ p50 | known- p50 | unknown p50 / p95 | 高危误配 ≥0.60 |
|---|---|---|---|---|
| g1→q3 (n=33/166) | 0.716 (n=25) | 0.829 | 0.747 / **0.897** | 140/166 (84%) |
| g3→q1 (n=166/33) | 0.879 (n=159) | 0.878 | 0.789 / **0.898** | 30/33 (91%) |

→ 库外个体分数与库内个体完全重叠，**预训练特征无拒识能力**（要 FA≤5% 需阈值 0.90，known recall 只剩 16-38%）。

**结果 2：微调特征（ArcFace 伪标签微调，注意：训练见过这些个体，数字为乐观估计）**

| 方向 | known+ p50 | known- p50 | unknown p50 / p95 | 高危误配 ≥0.60 |
|---|---|---|---|---|
| g1→q3 | 0.486 (n=25) | 0.516 | 0.332 / 0.562 | 5/166 (3.0%) |
| g3→q1 | 0.692 (n=159) | 0.559 | 0.488 / 0.640 | 6/33 (18%) |

→ unknown 分布明显下压（p95 0.897→0.562 / 0.898→0.640），拒识能力初步成立；推荐阈值（FA≤5%）0.6–0.7，known recall 36–47%——**已知个体仍约半数被误标为疑似新个体**，01 群 known+/known- 仍重叠（closed-set 内部难分，与 E1 同群结论一致）。

**结论与工程含义**：
- "疑似新个体"判定必须用微调特征；预训练特征直接拒识会全部误配；
- 当前微调特征下：流程可行（FA 3-18%），但"大大减少确认步骤"尚不成立——一半已知个体会进"疑似"待人工分辨；提升方向 = 跨群 hard negative 微调（TASKS 5.6）+ 更多确认数据 + 簇级检索（多帧投票）替代单图阈值；
- 语义边界：跨群预演 ≠ 跨时间新个体；微调特征含泄漏，known+ 是乐观估计；
- 产物：`outputs/reports/openset_preview_pretrained/` + `openset_preview_metric/`（metrics.json / threshold_curve.csv / unknown_detail.csv / 分布图）。

## 2026-08-17 实验 E4：跨群 hard negative 微调（r3）

**背景**：E3 结论是"微调特征有拒识能力但 known recall 仅 36-47%"。本实验针对 E3 短板做跨群 hard negative 微调。挖掘结果意外显示：r2 特征下跨群高分对极少（≥0.5 仅 13 对）——E3 瓶颈不在"跨群对不够低"而在"同体对不够高"。因此主机制改为 **batch 内动态 hard negative mining**：每 batch 强制 01/03 两群各半采样（01 仅 19 张，带替换重采样），triplet 的 negative = batch 内跨群最高 cos（动态 hardest），loss = ArcFace CE + 0.2×triplet（margin 0.3）。初始化自 r2/best.pt（head 重建）。脚本 `scripts/train_metric_learning_hn.py`（含预缩放内存缓存，原图 3000px 直接解码会卡 CPU）。

**训练**：116 张 / 25 个体（01:19, 03:97），验证 17 张 / 6 个体（模型未见）；val R@1 0.706（r2 权重初始）→ **0.765**（epoch 12）——独立验证个体提升，无过拟合信号。

**结果 1：B 协议 leave-one-out（r2 → r3）**

| 指标 | r2 | r3 |
|---|---|---|
| R@1 | 0.473 | **0.495** |
| R@5 | 0.728 | **0.783** |
| R@10 | 0.793 | **0.848** |
| mAP | 0.541 | **0.609** |

**结果 2：A 协议（代表图查询，真实场景）**

| 指标 | r2 | r3 |
|---|---|---|
| R@1 | 0.327 | **0.410** |
| R@5 | 0.564 | **0.699** |
| mAP | 0.435 | **0.533** |

**结果 3：E3 预演复跑（开放集拒识）**

| 方向 | known+ p50 | known- p50 | unknown p95 | FA≤5% 时 known recall |
|---|---|---|---|---|
| g1→q3 | 0.486→**0.511** | 0.516→**0.344** | 0.562→**0.489** | 0.36→**0.52** |
| g3→q1 | 0.692→0.689 | 0.559→**0.505** | 0.640→**0.566** | 0.47→**0.61** |

**关键改善**：dir 1→q3 的 known+ (0.511) 首次**超过** known- (0.344)——r2 时同体低于异体（0.486<0.516），无法区分；现在三分布分离度 0.17。推荐阈值（FA≤5%）降为 0.5–0.6，known recall 提升 16/14pp。

**结论**：
- 跨群 hard negative 微调对 E3 短板全面见效：拒识更强（unknown/known- 下压）、已知个体召回提升（36-47% → 52-61%）、检索全面提升（A 协议 mAP +9.8pp）；
- 验证个体 R@1 同步提升 → 提升来自特征泛化而非过拟合；
- "疑似新个体"流程的技术底座推进一大步；"大大减少确认步骤"仍需簇级检索与更多数据；
- 产物：`outputs/metric_learning/r3/`、`outputs/embeddings/embeddings_metric_r3.npy`、`outputs/reports/openset_preview_r3/`、`outputs/reports/benchmark_r3/`。

## 2026-08-17 实验 E5：簇级检索（多帧投票 vs 单图）

**背景**：E4 后单图已知个体召回 52-61%，剩余 4 成被误标为"疑似"靠模型再调压不动（数据天花板）。簇级检索是纯工程杠杆：真实归档流程中，新批次先批内聚成 Candidate Cluster 再匹配历史库，簇内多帧一致命中的置信度远高于单图孤证。用现有数据验证"多帧投票是否显著提升匹配与拒识"。

**设计**：探针/库拆分（HappyWhale 同款）——每个多图个体的照片按 seed 对半拆成 query 簇与库照片，单图个体整图入库，正确个体必在库中（R@1 有定义）。簇-个体分数 = 图对个体库照片 max cos，簇内 mean 聚合；单图对照 = 同一批探针图逐张独立 Top-1。特征 = r3（`embeddings_metric_r3.npy`）。脚本 `scripts/eval_cluster_retrieval.py`。

**结果 1：库内簇级匹配（个体级 R@1，多帧投票 vs 单图）**

| 群 | 探针簇数 | 簇级 R@1 | 单图 R@1 | 簇级 mAP | Top1 一致票中位 |
|---|---|---|---|---|---|
| 01 | 6 | 0.667 | 0.542 | 0.750 | 0.62 |
| 03 | 22 | **0.727** | 0.647 | 0.776 | 0.87 |

→ 多帧投票稳定提升 8-12pp；03 群簇内 87% 的图把正确个体排第一，投票高度一致。

**结果 2：跨群簇级拒识（03 簇 → 01 库，FA≤5% known recall）**

| 单元 | known p50 | unknown p50 / p95 | FA≤5% known recall |
|---|---|---|---|
| 图级（E3/E4 同口径，复现成功） | 0.511 (n=25) | 0.254 / 0.489 (n=166) | 0.520 |
| 簇级 | 1.000 (n=6) | 0.427 / 0.573 (n=29) | 1.000 |

**发现 1：known+ 双峰结构**。01 群同体照片 cos 呈现强双峰：相邻帧对 ≈1.0（同一次目击连拍），跨姿态照片对仅 0.1-0.5。E4 图级 known+ p50=0.511 是被大量低分姿态对拉低的中位；簇级聚合（簇内 mean）天然托起高分帧——这是多帧投票有效的机制解释。

**结论**：
- 多帧投票在库内匹配上稳定提升 8-12pp（03 群 n=22 较可信）；拒识方向一致（图级 52% → 簇级 100%）但 **known 侧 n=6 样本过薄**，仅作方向证据；
- 限制：探针簇 = 个体照片 = 理想纯净簇（真实 HDBSCAN 簇含噪声，实际低于此）；01/03 多图个体全部被 r3 训练见过（训练泄漏），数字为乐观估计；
- 工程含义：归档流程采用"簇级投票 + 簇内一致性证据"（簇 Top1 个体 + 一致票占比 + 分数），单图低分个体也可由簇级分数兜底；簇级拒识阈值可放宽（0.58 vs 图级 0.50）——簇级下未知个体整体分数抬升，阈值需按簇级重标定；
- 产物：`outputs/reports/cluster_retrieval/metrics.json`。

## 2026-08-19 实验 E7：跨时间批次管线首跑（真实多批次数据）

**背景**：数据提供方到位 9 个批次（1040 张，命名规范 `YYYYMMDD NN`）：20140806 01/03（旧数据改名，历史库）+ 20140418 01、20140419 02/04/05/06、20151017 02/03（新批次）。首次真实验证 A11 跨时间假设（不同日期批次重复拍摄到同一群/个体）与"新个体发现"流程。历史库 = 20140806 01/03 的 labeled（43 组 202 张，Candidate 级：Anchor 组 = 历史库个体，与 E3-E5 口径一致）。

**流程**：`scripts/run_cross_time_batch.py`（新脚本）——历史库 YOLO 裁剪 + r3 特征 → 新批次逐个跑 pipeline_archival（检测→特征→HDBSCAN→簇级匹配历史库→代表图+拼图）。

**过程中发现并修复的问题（记录备查）**：
1. **relative_path 前缀缺失（重要）**：scan_dataset 的 manifest relative_path 不含 session 前缀（`70-79/05/x.JPG`），而 detect_and_crop / pipeline_archival 的输入清单约定含前缀（与 pilot_set 一致，`session_id + "/" + relative_path`）。驱动脚本首版漏拼前缀 → 202 张全部"原图不存在"（初判误以为是 sj of 特殊目录的单张问题）。修复：gallery 与 query 清单统一拼 session 前缀。
2. **extract() merge 列缺失**：`--input-manifest` 模式把任意清单当 pilot_csv 传给 extract，硬性 merge pilot_set 全列 → KeyError；修复为只 merge 实际存在的列（`p[merge_cols]`），且裁剪清单自带 session_id 时跳过 merge 避免同名冲突。
3. **全噪声批次 summary KeyError**：0 候选簇时 `cluster_rows` 为空，`cm["status"]` KeyError；修复为回退到逐图 status 统计。
4. **session 命名规范升级**：旧白名单（`session_id = session_guess if in {"01","03"}`）→ 通用化（目录名即 session 标识）；extract_r3_yolocrop 的散图 session 正则 `^(0[13])/` → 路径首段解析；assign_pool 的 `map({1:"01",3:"03"})` → `_sess_key` 归一化（兼容 int/float/字符串）。
5. **nn relationship / sj of 语义确认**（数据提供方）：nn = **疑似**亲缘（一图两鳍）；sj of = **确定**亲缘。scan_dataset 分别标记 relation_note。
6. **20140419 04 批次别名**：用户已手动统一为 70-79 / 80 and above 标准命名。

**数据规模（2026-08-19 扫描）**：1040 张 = labeled 352（75 组）/ loose_known 322 / ignored 366（其中 nn_relationship 364 张，sj_of 9 张，均来自数据提供方确认）。

**结果（7 个新批次 270 张 → 20140806 历史库 43 组）**：

| 批次 | 张数 | 簇内图 | 噪声 | 簇级 match | 簇级 suspected | 噪声单图≥0.50 |
|---|---|---|---|---|---|---|
| 20140418 01 | 6 | 0 | 6 | 0 | 0 | 0 |
| 20140419 02 | 81 | 25 | 56 | 0 | 3 | 29 |
| 20140419 04 | 10 | 0 | 10 | 0 | 0 | 2 |
| 20140419 05 | 28 | 20 | 8 | 0 | 3 | 3 |
| 20140419 06 | 38 | 28 | 10 | 0 | 3 | 3 |
| 20151017 02 | 89 | 60 | 29 | 0 | 6 | 8 |
| 20151017 03 | 18 | 10 | 8 | **1** | 1 | 4 |
| 合计 | 270 | 143 | 127 | 1 | 16 | 49 |

**发现：首个真实跨时间匹配（A11 证据）**——20151017 03 簇 0（3 张连拍 0079/0080/0081，帧号 0137-0139，同一次目击）→ 历史个体 **20140806 03_1.0**（2014-04-17 HBi_03 群"01"组 2 张），簇级 score 0.6495（> 阈值 0.58），vote1 0.67（3 张中 2 张图级第一为该个体）。相隔约 1.5 年的两个批次照片被自动关联——**首个真实跨时间匹配（A11 证据，Candidate 级推定，须人工核验）**（注：2026-08-23 人工复核后该匹配已撤回，见 E9 更正）。

**其余簇**：16 簇 suspected_new（簇级分数 0.44-0.55，r3 特征空间现状，语义"宁可多标疑似"）；127 张噪声中 49 张单图匹配 ≥0.50（noise_match_candidate）。审核材料已生成：每批 clusters.csv（逐图）/ cluster_matches.csv（簇级汇总+投票证据）/ representatives/（代表图）/ contact_sheets/（拼图）。

**结论**：跨时间管线全链路真实跑通（扫描 → 裁剪 → 特征 → 聚类 → 簇级投票匹配 → 审核材料）；首个跨时间 match 的投票一致性 + 分数结构符合 E5 簇级语义预期。suspected_new 占比高是特征空间现状（E4 图级 known recall 52-61% 呼应），阈值与人工审核流程待数据提供方确认后推进（TASKS 6.4/6.5）。



**背景**：E5 用理想簇（个体照片）验证了多帧投票有效。本实验把流程接成真实管线：新批次（散图池 202 张为替身）→ YOLO 检测裁剪 → r3 特征 → HDBSCAN 批内候选聚类 → 每簇与历史库多帧投票匹配 → 输出归档候选 + 疑似新个体 + 噪声。脚本 `scripts/pipeline_archival.py`（可换任意批次清单跑完整流程）。

**配置**：散图池 202 张（01:54 / 03:148），r3+YOLO 裁剪特征，HDBSCAN min_cluster_size=3，簇级阈值 0.58（E5 标定参考值），噪声逐图单帧匹配阈值 0.50（E4 区间下限）。

**结果**：

| 项 | 数量 |
|---|---|
| 候选簇 | 8（含 25 张与 57 张大簇） |
| 噪声 | 89 张（44.1%，HDBSCAN -1 合法） |
| 簇 → match（≥0.58） | 2 簇（9 张，vote1 中位 0.875） |
| 簇 → suspected_new | 6 簇（104 张，score 0.44-0.54） |
| 噪声单图 ≥0.50 | 39/89 张 |

**验证**：
- 聚类语义正确：抽查簇 0 成员为连拍帧（帧号 1648/1651/1655/1656 连续）；cluster 3 全票一致（vote1=1.00, 0.696）→ match，符合预期；
- 噪声不合并：噪声点互不相似，修正为逐图独立单帧匹配（首版曾错误合并为"噪声簇"，已删除）；
- 代表图：每簇选与簇均值特征最近的一帧，输出至 representatives/（归档链路），另生成候选簇拼图供人工逐簇审核。

**发现**：
- 散图池在 r3 特征下簇级分数普遍偏低（0.44-0.55），多数簇标 suspected_new —— 与 E4 图级 known+ p50≈0.51 一致，是特征空间现状而非故障；0.58 阈值语义为"宁可多标疑似"（防错并优先）；
- 大簇（57 张）与帧号跨度大的簇（如簇 5：帧号 0047-0491）可能是同体多次目击（A13 散图有主）或相似个体混簇——**必须人工核验**，管线已输出拼图与成员清单；
- 噪声单图 ≥0.50 占 44%，与 E4 图级 known recall 52-61% 呼应。

**结论**：簇级真实流程可运行且语义正确（候选簇 / 噪声 / 候选划归，全部可追溯）；当前瓶颈仍是"低分待审量大"（数据天花板），管线提供审核材料（拼图 + 成员 + 投票证据）加速人工处理。真实新批次到达后可直接用 `--input-manifest` 跑完整流程。产物 `outputs/cluster_archival/pool_v2/`。


## 2026-08-20 实验 E8：两阶段聚类（子簇化）

**背景**：E7 的候选簇审核时发现大簇可能混入相似个体（簇 = Candidate Cluster，不可直接当个体）。改为两阶段聚类：第一阶段 HDBSCAN(min_cluster_size=3) 得到候选簇；第二阶段对每张大簇（≥4 张）内部再聚类（min_cluster_size=2, min_samples=1）拆成**纯子簇**（subcluster ≥ 0）与**残噪**（subcluster = -1，逐张审核）。审核单元从"整簇"细化为"纯子簇"。

**结果（6 个重跑批次，270 张中 118 张簇内图）**：

| 批次 | 簇内图 | 噪声 | 纯子簇 | 残噪 |
|---|---|---|---|---|
| 20140419 05 | 20 | 8 | 2 | 7 |
| 20140419 06 | 28 | 10 | 3 | 5 |
| 20151017 02 | 60 | 29 | 9 | 13 |
| 20151017 03 | 10 | 8 | 2 | 0 |
| 20140418 01 | 0 | 6 | 0 | 0 |
| 20140419 04 | 0 | 10 | 0 | 0 |
| 合计 | 118 | 71 | **16** | **25** |

（20140419 02 保留旧版未重跑）

**关键案例（方法价值）**：20151017 03 的簇 1.0 子簇化后显形出历史匹配候选 **20140806 03_5.0**（2 张全票一致）——该匹配在未拆分前被大簇掩盖，子簇化使隐藏候选显形；说明"拆簇显形"机制有效（注：该候选 2026-08-23 人工复核撤回，见 E9 更正——显形 ≠ 真匹配）。

## 2026-08-21/23 实验 E9：跨时间批次人工审核（真实验证）

**背景**：7 个新批次全部人工审核（审核网页 + 历史库对照 + 低质量对照隐藏功能；低质量判定：未检出背鳍 / det_conf<0.3 / Laplacian 方差 < p10 → 42/202 张，20.8%；质量表经 basename 重名映射修复后重算，见下）。

**结果（270 张全部过审，267 张标注）**：

- 确认组 **69 组**（198 张）、不确定 **62 张**、排除 **7 张**、未标注 3 张（20151017 02 残噪）
- **跨年匹配 3 条（人工确认，2014-08-06 历史库 → 2015 批次）**：
  1. `20140806 03_1.0` ← 20151017 03 簇 0.0（3 张）——模型候选（score 0.65、投票 2/3）确认
  2. `20140806 03_5.0` ← 20151017 03 簇 1.0（2 张）——子簇化新发现，全票确认
  3. `20140806 01_2.0` ← 20151017 02 噪声池（1 张）——模型未推荐，人工发现
- 模型候选精度：管线共提 4 个匹配候选 → 人工 2 对 2（防错并优先策略下人工审核不可省略）
- 62 张不确定集中在低置信单元（20140419 05 噪声/残噪、20151017 02 噪声），原则：看不清不下结论，不进确认库

**结论（2026-08-23 更新）**：
- ~~跨时间识别管线端到端真实验证通过：2015 年照片成功认出 2014 年记录个体~~——初审 3 条跨年匹配已全部撤回（审核失误，见下方更正），当前跨时间识别 **0 条确认**
- 管线端到端流程可跑通（检测→特征→聚类→投票→审核），候选仅提速参考；"加速人工"而非"替代人工"的定位成立
- 跨年匹配须待历史库核验 + 双人复核重审后才可重建
- 历史库 43 组 202 张为 Candidate 级假设，核验包已生成（batches_history/history_verify.csv，202/202 带特征辅助——basename 重名映射 bug 已修复：dataset_manifest 中同一 basename 在 nn relationship/ 目录重复（37 个重名），原简单 dict 映射使 9 张历史照片错配 image_id；修复为「basename+组名双条件 → 非 nn manifest → 兜底」后 202/202 全映射），待人工核验 + 双人复核重审后才可重建跨年匹配（见下方更正）
- 产物：`outputs/review_package/`（数据包 + 逐张审核记录 + 汇总）、`outputs/report_20260823/`（汇报图集 + 汇报材料）

### 更正（2026-08-23）：跨年匹配 3 条全部撤回

- **事件**：数据提供方复核判定初审的 3 条跨年匹配（03_1.0 / 03_5.0 / 01_2.0）均为**审核失误**。
- **处置**：6 张照片标注改为 `uncertain`（重新判定期）；审核汇总、03 批报告、汇报材料、图表全部同步撤回；
  match_evidence.png 与照片清单删除；模型候选裁决更新为"4 个候选全部未通过（2 初审撤回 + 2 否决）"。
- **新口径**：270 张 = 确认 **192 张 / 65 组**、不确定 **68 张**（含撤回 6 张）、排除 7、未标注 3、**跨年匹配 0 条**。
- **教训**：
  1. 跨年匹配必须**双人复核**（初审者自审可能受模型候选/组名先入影响）；
  2. 历史库 43 组核验完成前，任何跨年匹配都只是 Candidate 级推定，不得当作成果；
  3. 初审结论与模型候选都不能作为最终真值——"候选全否决"是诚实且正确的状态。
- **归因**：0 条确认不单纯是模型精度问题，更可能是数据覆盖不足——历史库 43 组全在 2014-08-06 一天，新批次仅 2014-04（5 天）/2015-10（1 天），"两次调查恰好拍到同一只"无先验保证；模型能力有独立证据（E5 同日簇级 R@1 0.667/0.727），跨年 0 确认 = 覆盖不足 + 跨年候选不可靠共同作用，占比在更多年份数据到位前无法区分（未确认 ≠ 不存在）。
- **下一步**：历史库核验（包已就绪）→ 双人复核重审 6 张 → 才可重建跨年匹配。

## 2026-08-25 实验 E10：背鳍轮廓特征原型 + A14 镜像对称性验证（4.3）

**背景**：为验证 A14（背鳍剪影左右近似对称，单侧个体可镜像检索跨侧同体）与 CurvRank 风格轮廓特征（缺口/凹陷/曲率）的可行性，在历史库 199 张 YOLO 裁剪图上做原型实验。

**配置**：
- 数据：历史库 20140806 01/03 labeled 199 张（YOLO 裁剪图，43 组：28 组多图 / 15 组单图；184 张属多图个体）
- 分割：灰度 Otsu + 最大连通域 + 形态学；自由边 = 顶部 45% 高度闭合多边形（背鳍判别特征在自由边）
- 特征（27 维）：曲率直方图 16（非对称版用有符号曲率；对称化版用 |曲率|）+ 缺口描述子 8（前 4 个缺口的深度/位置，按有向面积统一轮廓方向）+ 比例 3（高宽比/曲率均值/方差）
- 度量：余弦相似度；检索口径 = 多图个体照片 leave-one-out（排除自身），同体 = confirmed_identity

**结果 1：A14 镜像对称性（特征级）**

| 指标 | 非对称特征 | 对称化特征 |
|---|---|---|
| 自身 原vs镜像 p50 | 0.969 | **1.000** |
| 同体跨图 原vs镜像 p50 | 0.765 | 0.793 |
| 跨体 原vs镜像 p50 | 0.758 | 0.787 |
| 同体/跨体区分准确率 | 0.519 | 0.519 |

同一轮廓镜像翻转后特征几乎不变（对称化 1.000）→ **A14 的"剪影左右对称"在几何层面成立**。

**结果 2：轮廓特征检索 vs 外观特征（R@1 / R@5，n=184）**

| 变体 | R@1 | R@5 |
|---|---|---|
| 轮廓（原图→原图） | 0.087 | 0.310 |
| 轮廓（镜像 query→原图） | 0.060 | 0.283 |
| 轮廓（对称化，原图→原图） | 0.092 | 0.299 |
| 轮廓（镜像 query + 镜像增强 gallery，排除自身镜像） | 0.092 | 0.304 |
| **外观（r3 yolo 裁剪，基线）** | **0.728** | **0.848** |

**结论（诚实负面结果）**：
- 轮廓特征区分度极弱：同体 原vs原 p50=0.789 ≈ 跨体 0.785（分布重叠，acc≈随机）；R@1=0.087 远低于外观特征 0.728；
- **根因是分割/特征提取不稳定而非个体无差异**：同体轮廓特征对相似度 0.78（与跨体一致）——Otsu 分割受水纹/光照/角度干扰，同一背鳍不同照片的轮廓漂移吞掉了个体信号；
- 当前原型无法验证"跨侧镜像检索"应用（连同侧同体检索都失败）；A14 的几何对称性成立，但应用价值须在分割质量改善后重新评估；
- 附带发现：镜像增强 gallery 若无排除"自身镜像列"会给出虚假高分（0.989→修复后 0.092），实验设计须排除该平凡命中路径。

**4.5 错误分析（外观特征 r3，同口径）**：R@1 失败 50/184（27.2%），失败分散在 19 个个体（无集中病灶）；失败案例 top1 相似度中位 0.532（属排序性失败：top1 分数不低但排错个体，多为连拍组内相似）。

**产物**：`experiments/contour_features/`（分割/特征/实验脚本）、`outputs/reports/contour_features/`（features.npz / metrics.json / fail_cases.csv / individual_rates.csv / error_analysis.json）、`outputs/diagnosis/contour_seg/`（分割与自由边可视化）。

**下一步**：① 分割质量是瓶颈——尝试更大自由边比例 / 更强分割（需标注辅助）后再评估轮廓特征；② 轮廓特征可作为融合通道备选（当前不可用）；③ A14 应用须等分割改善，或改用深度特征验证镜像鲁棒性（见 E11）。

### E10 后续：自由边比例扫描（cut_ratio 0.45 / 0.55 / 0.65）

**问题**：E10 结论"分割不稳定是瓶颈"——是否只是自由边取太少（45%）导致判别信息不足？

**配置**：同 E10（199 张，Otsu 分割每张一次，仅自由边比例不同）；脚本 `experiments/contour_features/free_edge_ratio_exp.py`。

**结果**：

| cut_ratio | 同体 p50 | 跨体 p50 | 区分 acc | R@1 |
|---|---|---|---|---|
| 0.45（E10 基线） | 0.789 | 0.785 | 0.519 | 0.087 |
| 0.55 | 0.806 | 0.789 | 0.535 | 0.054 |
| 0.65 | 0.802 | 0.797 | 0.524 | 0.043 |

**结论（诚实负面结果）**：加大自由边比例**不改善**区分度——同体/跨体分布仍几乎重叠（acc≈0.52-0.54），R@1 反而下降。多取的轮廓（更低位置）无个体判别信息且带噪声。**瓶颈确在 Otsu 分割本身（同体轮廓漂移）而非自由边范围**；分割改进需走更强方法（SAM / 边缘检测 + 活动轮廓，需标注辅助），E10 结论维持。

**产物**：`outputs/reports/contour_features/free_edge_ratio.json`。

## 2026-08-25 实验 E11：外观特征水平翻转鲁棒性（4.2 增强策略）

**背景**：训练时水平翻转增强是否安全？若翻转后同体匹配相似度大幅下降，说明特征与侧别强相关，翻转增强会破坏标签语义；若几乎不变则翻转安全。同时直接回答 A14 的深度特征侧验证（跨侧观测到的图 = 水平翻转的图）。

**配置**：历史库 199 张裁剪图，ImageOps.mirror 生成翻转图；r3 微调权重提取翻转特征（MegaDescriptorMetricAdapter，与正式特征源同模型同预处理）；对照 = 现有 r3 原图特征。

**结果**：

| 指标 | 原vs原 | 原vs翻转 | 变化 |
|---|---|---|---|
| 同体相似度 p50 | 0.363 | 0.334 | **-0.030** |
| 跨体相似度 p50 | 0.057 | 0.060 | +0.003 |
| 同体/跨体区分 acc | 0.779 | 0.765 | **-0.014** |
| 翻转 query 检索 R@1 / R@5 | 0.728 / 0.848（原图基线） | 0.690 / 0.842 | -0.038 / -0.006 |

**结论**：
- **水平翻转对 r3 外观特征影响很小**：同体匹配相似度仅降 3 个点，区分准确率仅降 0.014，翻转 query 检索 R@1 降 3.8 个点——**水平翻转作为训练增强基本安全**（侧别信息损失可接受，未破坏同体匹配能力）；
- 深度特征对镜像有一定天然鲁棒性（背鳍对称 + 训练数据 side 全 unknown 未强化侧别），但 R@1 仍有 0.038 的降幅，跨侧检索仍须保守（作为 A14 应用的深度侧证据：支持镜像检索尝试，但阈值须单独标定）；
- 设计含义：4.2 增强策略可纳入水平翻转（概率小值，如 0.3），且翻转不须计入"侧别标签"语义。

**产物**：`experiments/flip_robustness.py`、`outputs/reports/flip_robustness/metrics.json`。

## 2026-08-25 实验 E5.1：全量库同体检索评估（E5 扩至 9 批次 1040 张）

**背景**：E5（2026-08-17）只覆盖 01/03 两群 199 张（当时数据未到位），库规模与真实场景差距大。数据全部到位后重测"同个体照片检索命中率"——这是判定模型是否具备个体识别能力的核心指标。

**配置**：
- 特征：全量 1040 张 → YOLO 检测裁剪（回退 135 张中心窗）→ r3 embedding（与正式特征源同链路），`outputs/embeddings/embeddings_eval51_all.npy`；
- 评估：E5 同款探针/库拆分（seed=7）——53 个多图个体（330 张 labeled）照片对半拆成 query 簇与库照片；单图个体（22 个）+ 散图/忽略图（688 张无标签）全部入库作干扰项；**库 = 889 张**（查询照片排除，防自身平凡命中）；
- 判定标准（强标签口径）：同体标签 = 当年分组文件夹的个体编号（individual_id），组内照片必属同一个体，本实验以此为命中的绝对标准；标签仅评分用（gallery 聚合/对答案），检索打分过程不使用标签；
- 脚本：`experiments/eval51_extract_all.py`、`experiments/eval_cluster_retrieval_v2.py`。

**结果 1：整体与分群（53 簇 / 151 张 query / 库 889 张）**

| 口径 | 单图 R@1 | 簇级 R@1 | 簇级 R@5 | 簇级 mAP | vote1 中位 |
|---|---|---|---|---|---|
| **整体（9 批次全库）** | 0.429 | 0.528 | 0.736 | 0.629 | 0.50 |
| 历史库 20140806 01/03（30 簇） | 0.339 | 0.467 | 0.700 | 0.576 | 0.08 |
| 新批次（23 簇） | 0.546 | 0.609 | 0.783 | 0.699 | 0.50 |

> 按 session 明细：20140418 01（1 簇 1.0）、20140419 02（0.714）、20140419 04（0.0，n=1）、20140419 05（1.0）、20140419 06（0.75）、20151017 02（0.50）、20151017 03（0.333）、20140806 01（0.25）、20140806 03（0.545）。

**结果 2：按"query 与同体最高相似度"分层（诊断性分析）**

| query 与同体最高 cos | n | 命中率 |
|---|---|---|
| (0.0, 0.3] | 18 | 0.000 |
| (0.3, 0.4] | 16 | 0.062 |
| (0.4, 0.5] | 22 | 0.227 |
| (0.5, 0.6] | 25 | 0.520 |
| (0.6, 0.7] | 19 | **0.895** |
| (0.7, 1.0] | 51 | **1.000** |
| 汇总 ≤0.5（组内不相似） | 56（37.1%） | 0.107 |
| 汇总 >0.5（组内确认相似） | 95 | 0.853 |

**发现**：
- **模型能力无瓶颈**：query 与同体照片相似度 >0.6 时命中率 89.5%，>0.7 时 **100%**（51/51）——凡"组内确实相似"的照片，模型几乎全部找回；
- **整体数字被组内不相似照片拖低**：37.1% 的 query 与同体最高相似度 ≤0.5（命中率仅 10.7%，其中 ≤0.3 的 18 张全败）。这些是当年分组内的跨目击/可能混入照片（例：20140806 01_1.0 组内连拍号 0108/0113 与 0168 相差 55 帧，IMG_6dc8 与前两张 cos 仅 0.15）——**属于标签分组质量问题（候选级分组未核验），非模型失败**；
- 与 E5（199 张小库）对比：簇级 R@1 0.667/0.727 → 0.467（历史库侧）。库扩 4.5 倍（干扰项 688 张）+ query 拆法使组内低相似照片进入测试 → 数字下降是真实规模下的预期；
- 历史库侧（0.467）低于新批次（0.609）：历史库 43 组为当年人工整理，组内跨目击照片多；新批次 labeled 多为同目击连拍（同体相似度高）；
- vote1 中位 0.08（历史库）：簇内大多数图的 Top-1 非正确个体，但簇级 mean 聚合仍能排对 46.7%——单图弱、聚合略有兜底，与 E5 known+ 双峰结构一致。

**结论**：
- 判定标准（当年分组 = 同体，强标签口径）下，模型对**真实同体照片**（组内相似 >0.6）的找回能力 89.5-100%——个体识别能力成立；
- 全量真实库规模（889 张）下整体 R@1 0.528 是"标签口径"下限：1/3 以上 query 是组内不相似照片（跨目击/混入），它们的失败不能归因于模型；
- 工程含义：① 1.9 散图整串划归的"多帧投票"策略仍成立（簇级 +8-10pp）；② 3.6 历史库核验的价值被量化——核验剔除组内混入/跨目击照片后，评估指标将显著回升；③ 检索阈值标定须在"组内相似照片"子集上做（避免被不可命中样本扭曲）；
- 诚实边界：r3 训练见过 01/03 部分个体（训练泄漏），历史库侧数字偏乐观；新批次侧（0.609）无泄漏更真实。

**产物**：`experiments/eval51_extract_all.py`、`experiments/eval_cluster_retrieval_v2.py`、`outputs/embeddings/embeddings_eval51_all.npy(+meta)`、`outputs/reports/cluster_retrieval_v2/`（metrics.json + per_individual.csv）。

### E5.1 后续：保守口径（剔除同串连拍平凡命中）

**动机**（评审指出）：query 与库照片若来自同一连拍串（连拍号差 ≤2），是平凡命中——相邻帧同一次目击、相似度天然高，不能证明个体识别能力（E5 已发现 known+ 双峰：相邻帧 ≈1.0 vs 跨姿态 0.1-0.5）。常规对半拆会让这类命中虚高数字。

**规则**：每张 query 打分前，从库中剔除与其同串照片（`exclude_same_series`，连拍号差 ≤2）；剔除后正确个体不在库中的 query 跳过（无法测跨目击）。脚本 `experiments/eval51_common.py` + `eval51_conservative.py`。

**同串平凡命中规模（新批次 64 张 query）**：命中 40 张中 **22 张（55%）是同串平凡命中**——常规口径数字过半虚高。

**结果（保守口径 vs 常规口径）**

| 口径 | 整体簇级 R@1 | 历史库簇级 R@1 | 新批次簇级 R@1 |
|---|---|---|---|
| 常规（对半拆，含同串） | 0.528 | 0.467 | 0.609 |
| **保守（剔除同串）** | **0.364** | **0.478** | **0.238** |

- 44 个探针簇可评估，10 张 query 跳过（该个体照片全在同一连拍串，剔除后库中无同体）；
- **历史库侧几乎不变**（0.467→0.478）：历史库组内照片本就是跨目击为主（同串影响小），0.478 是"跨目击同体识别"的真实能力证据；
- **新批次侧暴跌**（0.609→0.238）：常规口径的 0.609 大半来自连拍平凡命中；真实跨目击识别率只有约 1/4——**当年分组内的"连拍串"照片不能作为能力证据**；
- 簇级投票仍比单图高（0.311→0.364，+5pp），但增益远小于常规口径（+10pp）；
- 诚实边界：保守口径下 0.238/0.364 仍是"当年分组"标签口径（组内可能混入异体，如 01_1.0 案例），实际能力界于"标签口径下限"与"分层诊断 >0.6 相似度时 89.5-100%"之间——**评估集必须人工核验（3.2）才能真正回答跨目击识别率**。

**结论**：① 常规对半拆评估须配同串剔除，否则数字虚高（55% 命中是平凡命中）；② 模型对"真实跨目击同体"（相似 >0.6）找回能力高，但数据里这类照片少；③ 评估集构建（3.2）应把"同串连拍"作为防泄漏约束（同串照片不得跨 query/gallery），与 E5.1 保守口径一致。

**产物**：`experiments/eval51_common.py`、`eval51_conservative.py`、`outputs/reports/cluster_retrieval_v2/metrics_conservative.json`、`eval51_new_batches_view.html`（新批次划分与命中对照展示页，人工可核对）。

### E5.1 后续：串抽样版（用户方案，替代"连拍大户进干扰池"）

**动机**（用户 2026-08-25 评审）：连拍串内每张照片都有程度差异，不能因连拍就整个舍弃（原"连拍大户进干扰池"方向作废）；但同串平凡命中的筛选仍必要。折中方案：**对连拍串做随机抽样减半**——每串随机挑出 ceil(串长/2) 张（11 张挑 ≤6 张），挑剩的进干扰池（在库但不构成个体候选）；挑出的照片以**整串**为单位随机分边进 query/库（多串个体兜底保证两侧都有）。

**规则细节**（`experiments/eval51_common.py` `split_probe_gallery_series`）：
- 串分组 = 连拍号排序后相邻差 ≤2 的传递闭包（1,3,5 → 同串）；无连拍号（999999）各自成串；
- 挑剩的进干扰池：在库（`gal_ind=""`）但不作命中候选 → **query 与库候选永远不同串，平凡命中被根除**，连拍照片的一半信息仍留在库特征中；
- 单串个体（全串连拍，10 个）照片全部入库存作检索目标、不出 query——无法拆出跨串 query，如实报告。

**验证**：98 张 query 图逐一核对，与库中同个体候选照片**同串违规 0 张**。

**结果（串抽样 vs 保守 vs 常规，seed=7）**

| 口径 | 整体簇级 R@1 | 历史库簇级 R@1 | 新批次簇级 R@1 | 单图 R@1（整体） |
|---|---|---|---|---|
| 常规（对半拆，含同串） | 0.528 | 0.467 | 0.609 | 0.429 |
| **保守（剔除同串）** | 0.364 | 0.478 | 0.238 | 0.311 |
| **串抽样（每串一半+整串分边）** | **0.419** | **0.435** | **0.400** | **0.341** |

- 43 个探针簇 / 98 张 query（对半拆的 151 张 → 抽样后约 2/3）/ 库 942 张（干扰池 91 张）；
- **新批次 0.400 介于常规 0.609 与保守 0.238 之间**：防住了同串平凡命中，又保留了串内差异照片的检索信息——比"剔除同串"更充分地利用数据（保守口径把所有同串照片的 query 一并纳入打分，串抽样每串只派一半当 query，双方同为"跨串检索"但 query 构成不同，故数字有别）；
- 历史库 0.435（保守 0.478）：历史库组内本就跨目击为主，同串照片少，串抽样对它的影响小；
- 单图 0.341 vs 簇级 0.419（+8pp）：多帧投票增益在新口径下仍然成立；
- 诚实边界：① 串抽样把每串一半照片送进干扰池，这些照片的"跨串检索证据"不再被测——评估样本比保守口径更少（98 vs 141），是"防泄漏"与"样本量"的权衡；② 抽样结果随 seed 波动（seed=7 已固定可复现）；③ 单串个体（10 个）无法作为 probe，仅贡献库方干扰/候选。

**结论**：① 串抽样版是"防平凡命中 + 保留连拍差异"的可用折中，新批次真实跨串检索 ≈0.40；② 与保守口径互补：保守 0.238 是"全部照片跨串"下限，串抽样 0.400 是"串内抽样的跨串"水平——真实跨目击能力应落在两者之间，取决于评估集最终人工核验（3.2）；③ 评估集构建（3.2）采用串抽样约束：同串照片不得跨 query/gallery（干扰池例外），且连拍照片可保留一半参与评估。

**产物**：`experiments/eval51_common.py`（`split_probe_gallery_series`）、`eval51_series_split.py`、`outputs/reports/cluster_retrieval_v2/metrics_series_split.json` + `per_individual_series_split.csv`。

## 2026-08-25 实验 E5.2：r4 重训（根除历史库训练泄漏）

**动机**：r3 训练见过历史库 01/03 群 31 个个体（116 张训练 + 17 张验证），E5.1 历史库侧评估是"背题成绩"，不能作能力证据。重训 r4 使训练/评估彻底隔离：**训练集 = 历史库全部 43 个体 202 张（模型将见过），评估集 = 新批次 32 个体 150 张（模型从未见过）**——新批次侧从此无任何训练泄漏。

**训练配置**（用户 2026-08-25 决定：直接用 individual_id 当训练标签）：
- 训练 37 个体 / 125 张，验证 6 个体 / 77 张（`split_by_individual`，seed=42，val_n=6）；训练标签 = `individual_id`（候选级，组内可能混入异体如 01_1.0 案例，噪音接受）；
- 参数与 r3 全同：两阶段（s1 冻结 backbone 训 head 20 ep → s2 解冻微调 25 ep）、跨群 HN triplet（λ=0.2，batch 内 01/03 对半）、init 自 r2；
- 数据准备：`experiments/prep_train_hist.py` → `outputs/pilot/pilot_set_train_hist.csv`（`confirmed_identity` 填 individual_id；session_id 重映射 1/3 供 `BalancedGroupSampler`）；
- **修复**：r2 的 head 形状 768×25 ≠ 新训练的 768×37，原 `strict=False` 直接报错；`training.py` 改为按形状过滤键（head 跳过、backbone 继承），对旧链路行为不变；
- 训练结果：预训练基线 val R@1 = 0.740 → 微调后 **0.818**（best epoch 19；r3 当年 0.765，验证集不同不可直接比，但过程健康）。

**评估对照（r3 vs r4，同一拆分与口径）**

| 口径 | 指标 | r3 | r4 | Δ |
|---|---|---|---|---|
| 常规（对半拆） | 整体簇级 R@1 | 0.528 | **0.566** | +0.038 |
| 常规 | 历史库簇级 R@1 | 0.467 | 0.533 | +0.066 |
| 常规 | 新批次簇级 R@1 | 0.609 | 0.609 | 0.000 |
| 常规 | 整体单图 R@1 | 0.429 | **0.492** | +0.063 |
| **保守（剔除同串）** | 整体簇级 R@1 | 0.364 | **0.477** | **+0.113** |
| 保守 | 历史库簇级 R@1 | 0.478 | 0.565 | +0.087 |
| 保守 | **新批次簇级 R@1** | 0.238 | **0.381** | **+0.143** |
| 保守 | 整体单图 R@1 | 0.311 | 0.377 | +0.066 |
| 串抽样 | 整体簇级 R@1 | 0.419 | 0.419 | 0.000（内部 4 个体互换，见下） |
| 串抽样 | 历史库簇级 R@1 | 0.435 | 0.435 | 0.000 |
| 串抽样 | 新批次簇级 R@1 | 0.400 | 0.400 | 0.000 |
| 串抽样 | 整体单图 R@1 | 0.341 | 0.311 | -0.030 |

**解读**：
- **核心收益 = 保守口径新批次 0.238 → 0.381（+14.3pp）**：模型在从未见过的个体上、剔除同串连拍后，真实跨目击识别率显著提升——这是 r4 最硬的能力证据（评估个体与训练个体完全隔离）；
- 保守口径整体 0.364 → 0.477（+11pp）、历史库 0.478 → 0.565（+8.7pp）同步上涨：r4 的"跨串检索"能力整体强于 r3；
- **r4 下保守口径（0.477）反超串抽样（0.419）**：r3 时代串抽样（0.419）> 保守（0.364），说明 r3 的跨串能力不稳定、依赖抽样挑选照片；r4 让"全部照片跨串"（0.477）超过"抽样照片跨串"（0.419）——**跨串检索从"抽样才能打中"变成"普遍可打中"**；
- 串抽样口径平均数逐位相同是巧合配平：43 个个体中 4 个簇级命中变化（02_5.0/03_6.0 变好，06_1.0/03_4.0 变差），历史库/新批次内部恰好一进一出；单图 0.341 → 0.311 是真变化；
- 常规口径历史库侧 +6.6pp 含"见得更多"效应（r4 训练集含全部 43 个历史个体，含 r3 没见过的 12 个），该侧依旧不作能力证据；
- 诚实边界：① 训练标签为候选级 individual_id（组内可能混入异体，噪音被模型吸收，方向未知但评估结果表明净效果为正）；② 新批次侧（保守 0.381 / 串抽样 0.400）仍以"当年分组"为真值，最终数字待 3.2 人工核验；③ r4 特征与 r3 最大差 0.108（L2 后），是实质更新非等值。

**结论**：① individual_id 可直接作训练标签，重训后跨目击识别显著提升（保守口径新批次 +14pp）——训练集扩大（25→37 个体）与标签统一的双重收益；② 训练/评估个体彻底隔离后，新批次侧数字（保守 0.381 / 串抽样 0.400）为当前最可信能力证据；③ 评估集构建（3.2）建议以串抽样约束 + 新批次为评估主体。

**产物**：`experiments/prep_train_hist.py`、`outputs/pilot/pilot_set_train_hist.csv`、`outputs/metric_learning/r4/`（best.pt + history + metrics.json + `training_curves.png` + `train_report.md`，由 `experiments/plot_r4_training.py` 生成）、`outputs/embeddings/embeddings_eval51_all_r4.npy(+meta)`、`outputs/reports/cluster_retrieval_v2/metrics{_conservative,_series_split}__r4.json`（注意双下划线为 r4 后缀）与三口径对照图 `eval51_r3_vs_r4.png`。训练代码修改：[training.py](src/whitewhale/reid/training.py) init_ckpt 按形状过滤键。

**维护（同日）**：清理 `outputs/` 下 19 个历史调试临时文件（`_tmp_*.py/.js/.log`，2026-08-19~23 遗留，未跟踪未引用，删除无影响）；生产链路 `configs/pipeline.yaml` 的 `reid_checkpoint` 仍指向 r3，切换 r4 待确认。
