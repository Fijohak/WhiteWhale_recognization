# 硬依赖（特征提取 + 客户端）
numpy
pandas
pillow
torch
torchvision            # 预处理 transforms（embedding.py / training.py 正式依赖）
timm                   # MegaDescriptor / DINOv2 backbone（embedding.py 正式依赖）
ultralytics            # YOLO 背鳍检测（detector.py / query.py 正式依赖）
pyyaml                 # configs/*.yaml 加载（config.py 正式依赖）
fastapi
uvicorn
python-multipart
hdbscan                # HDBSCAN 候选聚类（run_pipeline 正式依赖）
filelock>=3.25,<3.26   # 审核票/累计导出 advisory lock（持久锁适配 3.25 API）

# 协作平台控制面（FastAPI + PostgreSQL）
sqlalchemy>=2.0,<2.1
alembic>=1.16,<2
psycopg[binary]>=3.2,<4
pydantic-settings>=2.10,<3
argon2-cffi>=25,<26
faiss-cpu>=1.12,<2

# 可选（按需安装）
# faiss-cpu            # Faiss Top-K 加速
# pytest               # 测试（tests/）
# opencv-python        # experiments/contour_features 轮廓特征实验
# scipy                # experiments/contour_features cKDTree 空间检索
# matplotlib           # experiments/eval_openset_preview 分布图绘制
