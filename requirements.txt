# 硬依赖（特征提取 + 客户端）
numpy
pandas
pillow
torch
timm
fastapi
uvicorn
python-multipart
hdbscan            # HDBSCAN 候选聚类（run_pipeline 正式依赖）

# 可选（按需安装）
# faiss-cpu        # Faiss Top-K 加速
# pytest           # 测试（tests/）
