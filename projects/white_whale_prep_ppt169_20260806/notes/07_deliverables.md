索引体系由四个核心文件构成，全部位于 outputs 目录下的 index 目录。第一个是数据清单，两千八百六十三行、二十八个字段，包含 image_id、标签、候选分组和哈希值。第二个是全量统计，覆盖标签分布、个体数和重复检测结果。第三个是目录树缩略预览，供人工核对目录语义。第四个是异常文件表，目前零损坏。整套体系保证原始数据只读、每张图片可追溯到原始路径、数据来源信息完整保留、脚本可重复运行。